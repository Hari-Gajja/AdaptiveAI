# Repository Cleanup Report

**Date:** 2026-03-01
**Method:** Safety-first — inventory → reference-check → classify → report → delete → verify.
**Rule applied:** "When uncertain: KEEP THE FILE." No destructive git commands used.

---

## 1. What was inventoried

Full tree of `llm-cost-optimizer/` (excluding `.git/`, `node_modules/`, `dist/`,
`__pycache__/`, `.venv/`), cross-checked against `git ls-files` (74 tracked files)
and `git status` (clean at commit `bd065e3`).

## 2. Reference checks performed

| Check | Result |
|---|---|
| `out.txt`, `server.log`, `server.err.log`, `shot-*.png` referenced in any `.py/.jsx/.js/.md/.json/.html`? | **No matches** — nothing reads or links them |
| `DEMO.md` referenced anywhere? | No — standalone demo script, kept as documentation |
| `backend/core/semantic.py` referenced? | Yes — `core/cache.py` + `test_cache_cost.py` import it |
| `backend/config.py` referenced? | Yes — 8 modules import it |
| `validate_phase1` in `config.py` used? | Yes — `test_provider.py` (kept) |
| `playwright` (frontend devDependency) used? | Only by workspace-root `test-playwright.js` (screenshot helper, outside repo) |
| `.pytest_cache/`, `__pycache__/` tracked by git? | No — generated, untracked |
| Screenshots referenced in README/DEMO? | No — not embedded in any doc |

## 3. Files removed (verified safe)

| File | Reason | Tracked? |
|---|---|---|
| `backend/out.txt` | 0 bytes, empty, nothing references it | No (untracked) |
| `server.log` | Runtime uvicorn access log (regenerates on every run) | No (untracked) |
| `server.err.log` | Runtime uvicorn startup log (regenerates on every run) | No (untracked) |
| `.pytest_cache/` (root) | Generated pytest cache | No (untracked) |
| `backend/.pytest_cache/` | Generated pytest cache | No (untracked) |
| `__pycache__/` dirs | Generated bytecode caches | No (untracked) |

## 4. Files kept (with reasons)

| File | Reason |
|---|---|
| `.env` | Live API key + config; intentionally untracked (gitignored) |
| `.env.example` | Template for setup; documented in README |
| `.gitignore` | Version control hygiene |
| `README.md` | Primary documentation |
| `DEMO.md` | 6-demo live script (~8 min); standalone doc, no code references it but it is the demo narrative |
| `backend/**` (all source) | Active architecture: one cache (`core/cache.py` + `core/semantic.py`), one router (`core/router.py`), one provider (`providers/opencode.py`), one FastAPI app (`main.py`), control plane (`llm/`), evaluation (`core/quality.py` + `llm/opencode_evaluator.py`) |
| `backend/core/task_analyzer.py` (legacy classifier) | **Intentional A/B**: `CLASSIFIER_BACKEND=legacy_ml` is a supported, tested path (`test_control_plane.py`); not dead code |
| `backend/core/semantic.py` | Imported by `core/cache.py` and `test_cache_cost.py` |
| `backend/config.py` | Central config; `validate_phase1` used by `test_provider.py` |
| 9 × `backend/test_*.py` | All pass at `bd065e3`; README documents 7 of them |
| `backend/benchmark/*.json` + `runner.py` | Benchmark dataset + runner (demo 6) |
| `backend/data/*.json` | Model registry + capability profiles |
| `frontend/**` (all source) | React dashboard (Vite 6) |
| `frontend/shot-*.png` (5) | **KEPT (uncertain → keep rule)**: untracked screenshots from `test-playwright.js`; not referenced in README/DEMO, but they are the only visual record of the dashboard; harmless (few hundred KB) |
| `frontend/package.json` / `package-lock.json` | Dependency manifest + lockfile |
| `backend/requirements.txt` | All 9 deps verified used (fastapi, uvicorn, httpx, python-dotenv, pydantic, pymongo, redis, fakeredis, numpy) |

## 5. Files uncertain (kept per safety rule)

| File | Question | Decision |
|---|---|---|
| `frontend/shot-*.png` ×5 | Demo screenshots — referenced nowhere, but only visual record | **KEEP** (uncertain → keep) |
| `test-playwright.js` (workspace root, outside repo) | Screenshot helper for the shots above | **KEEP** (outside repo scope; regenerates the shots) |

## 6. Architecture conformance (cleanup prompt §3)

| Prompt expectation | Repo state | Action |
|---|---|---|
| One cache | `core/cache.py` + `core/semantic.py` (one tier pair) | Conforms — nothing to remove |
| One routing policy | `core/router.py` only | Conforms |
| One provider | `providers/opencode.py` only | Conforms |
| One evaluation path | `core/quality.py` + `llm/opencode_evaluator.py` | Conforms |
| One benchmark runner | `benchmark/runner.py` only | Conforms |
| One FastAPI app | `main.py` only | Conforms |
| Legacy classifier | `core/task_analyzer.py` legacy path | **Retained intentionally** (A/B via `CLASSIFIER_BACKEND=legacy_ml`, tested) |
| Mock files / duplicate routers / old cache versions | None exist | Nothing to remove |
| `prompts/` dir | Prompts are inline in Python | Nothing to remove |
| `scripts/` dir | None | Nothing to remove |
| Model artifacts (`.pkl/.joblib/.npy`) | None | Nothing to remove |
| RouteLLM adapter | None | Nothing to remove |
| Streamlit | None — React dashboard instead | Structure may differ; not forced |

## 7. Dependency audit

- `backend/requirements.txt`: 9/9 packages used. **Nothing to remove.**
- `frontend/package.json`: all deps used (`lucide-react`, `react`, `react-dom`,
  `@vitejs/plugin-react`, `vite`); `playwright` is a devDependency used only by
  the workspace-root screenshot helper. **Nothing to remove.**

## 8. Deletion method

Untracked generated files deleted with `Remove-Item` only. **No `git clean`,
no `git reset --hard`, no force-push, no history rewrite.** All deletions are
of untracked files, so `git status` remains clean.

## 9. Post-deletion verification (see final report)

- All 9 test files re-run → expected ALL PASS
- Backend restart + `/health` + `/api/models/control-plane` → expected 200
- Frontend `npm run build` → expected success
- `git status` → expected clean
